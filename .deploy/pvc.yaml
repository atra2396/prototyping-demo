apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: contact-service-pvc-argo-2
spec:
  resources:
    requests:
      storage: 10Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany